


SIMULATE SYSTEM 3 FOR RANDOM TDMA

paper ---> 
		* file:///Users/anthonykassis/Documents/RTDMA-ALOHA.pdf


RESOURCES:
	passive star : --> https://en.wikipedia.org/wiki/Star_coupler 



DEFFINITIONS
		STAR COUPLERS :  	ports must be powers of 2 , x input x output 
					Use DCL for configuring and programing ????>>a

		NODE STATES : BUSY ---> AT LEAST ONE PACKET IN THE BUFFER IN TIME INTERVAL t_j
			      LAZY ---> EMPTY BUFFER  IN TIME INTERVAL t_j
			

		SYSTEM 3 :   8 Tx  - > Star - > 8 Rx    T_i = {λ1,λ2,λ3,λ4} 4 possible wavelengths  FOR ALL i 
			     R_i is {1} for i = 1,2 ;R_i= {2} for i=3,4 ... R_i = {4{ for i = 7,8
	
				CONSTRAINTS THAT MUST BE SATISFIED : --> 
						1) EACH W channel (here 4 ) must have at least one Tx assigned .
						2) EACH W channel (here 4 ) must have at least one Rx assigned .
						3) single hop for each Tx - Rx meaning --->
			
				-----------

			     HOW MAKE GWORK??
				3 phases 
					1) fill the buffer in TX
					2) send the buffer in the STAR
					3) check if correct packets arrived in the RX
	
		
 				EACH TX node gets a 0-L length Markov_chain of random packets that fill the buffer.

		

NOTES
------------------------ FIRST ATTEMPT EACH t_j a Tx can have A SINGLE AND ONLY ONE λ that sends and RX node is known 


0-- > SYSTEM


 N nodes connected to STARcoupler , W channels with w<=N  EACH CHANNEL HAS ITS OWN lamda.

	**	N_i (a node i)  has a Tx that can send to different W channels (tunable laser that can tx in different lamdas denoted as T_i set);
	**	N_i has a set of lamdas R_i one of the FIXEEED  Rx connected to N_i  can accept  

			IF WE HAVE the W channels then each channel has a set of N_i nodes that can send shit through  and a set of R_i nodes that can receive shit from it it at a given time ( this can change>?>???);;
			--> we donote the set of N_i and R_i for a given W as (A_w1 ,B_w1) , (A_w2,B_w2) ... (TX_nodes_w_λι , RX_nodes_w_λι)
		
	**      N_i has a buffer of length L .
		
	** 	Time is in timeslots or time_intervals ..t_j;
		 	




1 -- > PROTOCOL 

	 RANDOM TDMA PROTOCOL:

			
		Each t_j repeat:
		
				--> Fill a trans list where each node i can transmit to channel w 
						e.g trans[2] = 4 , trans[5] = 0 , trans[1]=4 , trans[8] = 1  nodes 2 and 4 send to wavelength λ4 ,node 5 sends fukall
													     node 8 sends to channel 1 (λ1) ...
													   
				--> MUST BE PREDETERMINED WHICH RX nodes can accept each channel  (MAKE AN RX list???)
						e.g RX[2] = 4 , RX[1] = 2 

				
					----------------------
					HOW FILL Trans[i] so no collisiton.

							-- > Omega = {1 , .. , N} set of nodes that TX

							-- > A = a set of sets , each set is the N_is that can transmit to Rx_is
							-- > A = {A_Rx_1 , A_RX_2 ,... }
									A_RX_1 = [TX2,TX5,TX1]
									A_RX_2 = [TX2 ,TX8]
									A_RX_3 = [TX4,TX5,TX8]
									.
									.
									.
					







							















